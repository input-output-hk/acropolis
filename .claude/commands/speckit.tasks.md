Read and execute the workflow defined in `.github/agents/speckit.tasks.agent.md`.

**Additional context:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command breaks down the implementation plan into actionable tasks.
