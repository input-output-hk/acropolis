Read and execute the workflow defined in `.github/agents/speckit.constitution.agent.md`.

**Additional context:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command validates the feature against project principles and constitution.
