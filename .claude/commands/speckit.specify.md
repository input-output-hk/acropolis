Read and execute the workflow defined in `.github/agents/speckit.specify.agent.md`.

**Feature description:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command creates a new feature specification from a natural language description.
