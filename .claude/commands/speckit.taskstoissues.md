Read and execute the workflow defined in `.github/agents/speckit.taskstoissues.agent.md`.

**Additional context:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command converts tasks from tasks.md into GitHub issues.
