Read and execute the workflow defined in `.github/agents/speckit.feedback.agent.md`.

**Arguments:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command extracts lessons learned from PR review comments and discussions.
