Read and execute the workflow defined in `.github/agents/speckit.implement.agent.md`.

**Additional context:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command executes the implementation plan by processing all tasks in tasks.md.
