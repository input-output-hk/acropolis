Read and execute the workflow defined in `.github/agents/speckit.plan.agent.md`.

**Additional context:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command generates the technical implementation plan for the current feature.
