Read and execute the workflow defined in `.github/agents/speckit.analyze.agent.md`.

**Analysis target:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command analyzes existing codebase or feature components.
