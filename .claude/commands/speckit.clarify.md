Read and execute the workflow defined in `.github/agents/speckit.clarify.agent.md`.

**Additional context:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command clarifies specification requirements and resolves [NEEDS CLARIFICATION] markers.
