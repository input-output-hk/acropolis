Read and execute the workflow defined in `.github/agents/speckit.checklist.agent.md`.

**Domain/context:** $ARGUMENTS

Follow the agent instructions exactly as written in the agent file. This command creates validation checklists for the feature implementation.
